version: 2
project_name: cyfr

builds:
  - main: .
    binary: cyfr
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/cyfr/codex/cmd.Version={{.Version}}
      - -X github.com/cyfr/codex/cmd.Commit={{.Commit}}
      - -X github.com/cyfr/codex/cmd.Date={{.Date}}

archives:
  - name_template: "cyfr_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: ['tar.gz']
    format_overrides:
      - goos: windows
        formats: ['zip']

checksum:
  name_template: checksums.txt
  algorithm: sha256

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - "sign-blob"
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

homebrew_casks:
  - repository:
      owner: cyfrworks
      name: homebrew-cyfr
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://cyfr.run"
    description: "CLI for the CYFR sandboxed WASM runtime"
    binaries:
      - cyfr
    # cyfr.run homepage differs from the github.com download domain — declare it
    # verified so `brew audit` passes.
    url:
      verified: "github.com/cyfrworks/cyfr"
    # Binaries are cosign-signed but not Apple-notarized, so macOS Gatekeeper
    # would quarantine them. Strip the quarantine xattr on install.
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/cyfr"]
          end
    # Generate the cask into dist/ but do not push it — the release workflow
    # pushes it in a separate, non-fatal step.
    skip_upload: true

release:
  github:
    owner: cyfrworks
    name: cyfr
  make_latest: false
  extra_files:
    - glob: cyfr-scaffold.tar.gz
