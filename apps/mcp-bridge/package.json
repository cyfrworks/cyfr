{
  "name": "cyfr-mcp-bridge",
  "version": "0.5.8",
  "private": true,
  "license": "Apache-2.0",
  "description": "Runs the stdio MCP servers CYFR registers with transport stdio, one owner per server row, and serves each owner's backend tools over HTTP MCP under requests signed with CYFR_MCP_BRIDGE_KEY.",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=24.2"
  }
}
