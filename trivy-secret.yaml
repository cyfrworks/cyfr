# Detect Google OAuth client secrets during Trivy secret scans.
rules:
  - id: google-oauth-client-secret
    category: Google
    title: Google OAuth client secret
    severity: CRITICAL
    regex: 'GOCSPX-[A-Za-z0-9_\-]{28}'
    keywords:
      - GOCSPX-
